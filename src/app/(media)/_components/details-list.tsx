import {
  MovieResponse,
  Person,
  ShowResponse,
} from "@/types/request-types-snakecase";

export function DetailsList({
  data,
}: {
  data: MovieResponse | ShowResponse | Person;
}) {
  return;
}
